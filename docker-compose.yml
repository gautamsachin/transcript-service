version: '3'

services:

  transcript-api:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - redis
    environment:
      REDIS_HOST: redis

  redis:
    image: redis
    ports:
      - "6379:6379"